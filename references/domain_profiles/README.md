# Domain Profiles / 领域配置

Core skills 必须保持 domain-agnostic。Domain hints 放在这里，并且只在相关任务中加载。

Examples / 示例:
- `npu.md`: NPU compute/datapath conventions
- `bus.md`: bus fabric and register block conventions
- `dsp.md`: DSP arithmetic conventions
- `memory.md`: SRAM/FIFO/buffer conventions
